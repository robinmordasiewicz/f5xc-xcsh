metadata:
  name: claude-test-pool
  namespace: default
spec:
  origin_servers:
    - public_ip:
        ip: 1.2.3.4
    - public_ip:
        ip: 5.6.7.8
  port: 80
  loadbalancer_algorithm: ROUND_ROBIN
  no_tls: {}
